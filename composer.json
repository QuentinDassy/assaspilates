{
    "name": "assas-pilates-ballet/backend",
    "description": "PHP API backend (site/api) for the booking/carnet system, deployed to OVH shared hosting via native Git deploy (no server-side Composer -- vendor/ is committed).",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "stripe/stripe-php": "^15.0",
        "firebase/php-jwt": "^7.0"
    },
    "config": {
        "vendor-dir": "site/api/vendor",
        "optimize-autoloader": true
    }
}
