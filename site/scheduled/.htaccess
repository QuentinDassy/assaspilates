# Defense in depth on top of cleanup-stale-holds.php's own PHP_SAPI check --
# OVH's cron feature invokes scripts via CLI, never over HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
